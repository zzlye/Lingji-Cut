# YouTube 视频处理桌面软件计划（修正版）

## Summary
“去重”模块改为“视频差异化处理/二创处理”，不是重复文件检测。软件流程调整为：

`解析 YouTube 视频 -> 下载入库 -> 视频差异化处理 -> 加字幕 -> 可选配音 -> 合成导出`

前端使用 `React + TypeScript + Vite`，桌面端使用 `Electron`，后端使用 `Python + FastAPI`。后端通过 `yt-dlp` 下载，通过 `ffmpeg` 执行画面调整、分辨率、旋转翻转、帧率、抽帧、动态缩放、码率、字幕和音频合成。

## Key Changes / Implementation
- 视频差异化处理模块：
  - 命名为“画面处理”或“视频增强”，不再叫“自动去重”。
  - 支持预设方案：轻度、标准、强处理、自定义。
  - 每个参数支持固定值或随机范围。
  - 每个处理项有独立开关、参数输入、预览按钮、重置按钮。
  - 支持保存多个处理模板，并在任务中复用。

- 画面调整能力：
  - 亮度：支持范围随机，例如 `0.00 - 0.10`。
  - 对比度：支持范围随机，例如 `1.00 - 1.20`。
  - 饱和度：支持范围随机，例如 `1.00 - 1.10`。
  - 锐化：支持范围随机，例如 `0.9 - 1.4`。
  - 降噪：支持范围随机，例如 `1 - 2`。
  - 所有参数最终由 Python 转成 `ffmpeg` filter graph。

- 分辨率与画布：
  - 支持分辨率选择：`720p`、`1080p`、原分辨率、自定义。
  - 支持模式：拉伸、裁切、原比例。
  - 支持背景填充、背景模糊、倒影。
  - 支持宫格分屏，默认先作为后续扩展。

- 旋转与翻转：
  - 支持左转 90 度、右转 90 度。
  - 支持水平翻转、垂直翻转。
  - 支持随机轻微旋转，例如 `-1.0° - 1.0°`。
  - 支持黑边去除和完整显示选项。

- 帧率与时长变化：
  - 支持帧率设置，例如 `30.0 - 30.0` 或随机范围。
  - 支持抽帧，例如每 `25 - 30` 帧抽一帧。
  - 支持动态缩放，例如 `0.01 - 0.02`。
  - 抽帧、动态缩放默认关闭，避免明显影响观看体验。

- 码率与清晰度：
  - 支持按倍率调整，例如 `1.05 - 1.95`。
  - 支持固定码率，例如 `2000 kb/s`。
  - 默认使用固定码率，便于控制导出体积。
  - 提供“保持清晰优先”和“控制体积优先”两个快捷方案。

- 字幕系统：
  - 支持 YouTube 原字幕/自动字幕。
  - 支持 API 生成、翻译、润色字幕。
  - 支持单行/双行、字体、字号、位置、颜色、描边、阴影、背景透明度。
  - 支持单独字幕颜色，例如按关键词、说话人、语言区分。
  - 默认生成硬字幕视频，同时保留 `SRT/ASS` 文件。

- API 配置系统：
  - 文本 API 支持 OpenAI、OpenAI-compatible、Gemini、Gemini OpenAI-compatible、Anthropic、自定义渠道。
  - 配音 API 支持 OpenAI TTS、Gemini TTS、自定义 TTS。
  - 配置支持保存、切换、测试连接。
  - API key 本地加密保存，日志中不输出密钥。

- 配音系统：
  - 配音为可选步骤。
  - 支持从字幕、翻译字幕、自定义文案生成配音。
  - 支持整段生成和按字幕分段生成。
  - 支持保留原声、降低原声音量、替换原声。
  - 最终由 `ffmpeg` 合成音频和视频。

## Public Interfaces / Types
- 后端 API：
  - `POST /videos/parse`：解析 YouTube URL。
  - `POST /videos/download`：创建下载任务。
  - `POST /effects/preview`：生成指定时间点或短片段预览。
  - `POST /effects/apply`：创建画面处理任务。
  - `GET /tasks`：获取任务列表。
  - `GET /tasks/{id}`：获取任务状态。
  - `POST /tasks/{id}/retry`：重试任务。
  - `POST /subtitles/render`：生成字幕文件或硬字幕视频。
  - `POST /voice/generate`：生成配音。
  - `POST /exports/create`：创建导出任务。
  - `GET/POST/PUT/DELETE /profiles`：管理 API 配置。
  - `POST /profiles/test`：测试 API 配置。

- 核心类型：
  - `VideoSource`：URL、平台、video_id、标题、作者、时长。
  - `ProcessingPreset`：画面调整、分辨率、旋转翻转、帧率、抽帧、缩放、码率配置。
  - `RandomRange`：最小值、最大值、是否启用随机。
  - `SubtitlePreset`：单双行、字体、颜色、描边、位置、透明度。
  - `TextProviderProfile`：文本模型配置。
  - `VoiceProviderProfile`：配音模型配置。
  - `ExportJob`：输入视频、处理参数、字幕、音频、输出路径。

## Test Plan
- 画面处理测试：
  - 每个处理项开关能独立生效。
  - 固定值和随机范围都能生成正确 `ffmpeg` 参数。
  - 预览片段和完整导出的效果一致。
  - 720p、1080p、原比例、裁切、拉伸都能正常导出。
  - 水平翻转、轻微旋转、帧率、码率调整后视频可正常播放。

- 字幕测试：
  - 字幕样式配置可正确生成 `ASS`。
  - 单行/双行、颜色、描边、位置在预览和导出中一致。
  - 硬字幕视频可正常播放，字幕文件会保留。

- API 测试：
  - 不同文本 API 配置可保存、切换、测试。
  - 配音 API 可生成音频并参与合成。
  - API 错误展示明确原因，不泄露 key。

- 桌面测试：
  - Electron 能启动 Python 后端。
  - 前端能实时显示任务进度、处理日志、错误原因。
  - `D:\tools\yt-dlp`、`D:\tools\ffmpeg` 存在时优先使用。
  - 打包后的 Windows 软件能在无开发环境下运行。

## Assumptions / Defaults
- 默认桌面壳使用 `Electron`。
- 默认前端使用 `React + TypeScript + Vite`。
- 默认后端使用 `Python + FastAPI`。
- 默认数据库使用 `SQLite`。
- 默认工具使用 `yt-dlp + ffmpeg`。
- 默认画面处理预设为“标准”：轻微亮度、对比度、饱和度、锐化、720p/1080p 输出、固定码率、硬字幕。
- 默认抽帧、动态缩放、宫格分屏关闭。
- 默认导出硬字幕视频，同时保留原视频、字幕文件、中间音频。
- 默认先做 Windows 版。
- 默认所有新增代码注释使用中文。
