# Python 后端依赖
# 安装命令：pip install -r requirements.txt

# Web 框架
fastapi>=0.115.0
uvicorn>=0.34.0

# 数据库
sqlalchemy>=2.0.0

# 数据验证
pydantic>=2.0.0

# 加密（apiKey 本地加密存储）
cryptography>=44.0.0

# HTTP 客户端（API 调用）
httpx>=0.28.0

# 可选本地识别模型：
# - SenseVoice 需要安装 funasr，并提前下载/缓存 SenseVoiceSmall 模型。
# - Qwen3-ASR 需要安装 qwen-asr，并提前下载/缓存 Qwen3-ASR 与 ForcedAligner 模型。
